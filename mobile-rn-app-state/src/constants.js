export const NAME = 'appState';

export const INIT_REQUEST = 'appState/INIT_REQUEST';

export const ON_APP_STATE_CHANGE = 'appState/ON_APP_STATE_CHANGE';

export const SET_APP_STATE = 'appState/SET_APP_STATE';

const publicConstants = {
  NAME,
  ON_APP_STATE_CHANGE
};

export default publicConstants;
