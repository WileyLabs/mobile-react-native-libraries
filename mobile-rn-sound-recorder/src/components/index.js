import Recorder from './Recorder/RecorderContainer';
import RecordButton from './Elements/RecordButtonContainer';
import SaveButton from './Elements/SaveButtonContainer';
import StatusArea from './Elements/StatusAreaContainer';

export const publicComponents = {
  Recorder,
  RecordButton,
  SaveButton,
  StatusArea
};

export default publicComponents;
